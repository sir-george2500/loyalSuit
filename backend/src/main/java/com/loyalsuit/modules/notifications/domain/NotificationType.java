package com.loyalsuit.modules.notifications.domain;

/** What a notification is about — drives the icon/tone the inbox shows. */
public enum NotificationType {
    ORDER_PLACED,
    ORDER_DELIVERED,
    PAYOUT_PAID,
    GENERAL
}
